{
  "devDependencies": {
    "autoprefixer": "^9.1.5",
    "cssnano": "^4.1.0",
    "husky": "^0.14.3",
    "parcel-bundler": "^1.9.7",
    "prettier": "^1.14.2"
  },
  "name": "aldreth-static",
  "scripts": {
    "precommit": "yarn prettier",
    "build": "rm -rf dist && parcel build index.html && cp _redirects dist/",
    "prettier": "prettier --write '+(css|js)/**/*.+(css|js)'",
    "start": "parcel index.html"
  },
  "version": "1.0.0",
  "dependencies": {}
}
